export const handleKeyUp = (e, value) => {
  if(e.key === 'Enter') {
    setSearch(value);
  }

};
export const handleKeyUpBuy = (e, value) => {
  if(e.key === 'Enter') {
    setSearch(value);
  }

};
